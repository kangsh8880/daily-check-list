// ============================================================================
// AI(LLM) 연동 설정 - 기본값은 비활성화(provider:"none")
// "부품유형/점검항목" 화면의 "점검항목 추천" 탭에서, 표준 라이브러리에 없는 생소한
// 부품유형을 등록했을 때에 한해 보완적으로 사용됩니다(라이브러리 매칭이 우선).
//
// ⚠️ GitHub Pages 같은 정적 사이트는 서버가 없어 API 키를 프록시 뒤로 숨길 수 없습니다.
//    아래에 키를 직접 넣으면 공개 저장소에 키가 그대로 노출된다는 뜻입니다.
//    사용하려면: 1) 발급처(Google AI Studio 등)에서 API 사용 범위를 최대한 제한하고
//    2) 이 위험을 인지한 상태에서 진행해 주세요.
//    (참고: Claude 프로젝트 "9988234 Project"의 대시보드_구축_원칙.md
//     "클라이언트에 API 키를 직접 넣는 경우의 원칙")
//
// 설정하지 않아도(provider:"none") 앱은 정상 동작합니다 - 이 경우 라이브러리에 없는
// 부품유형은 "일반 참고용" 항목으로 대체 표시됩니다.
// ============================================================================
window.AI_CONFIG = {
  provider: "none",     // "gemini" | "groq" | "none"
  geminiApiKey: "",     // 발급: https://aistudio.google.com/apikey
  groqApiKey: ""        // 발급(무료 대안): https://console.groq.com/keys
};
